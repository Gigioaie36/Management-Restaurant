<UserControl x:Class="RestaurantManager.Wpf.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Reports Dashboard" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Refresh" Command="{Binding RefreshReportsCommand}" Padding="10,5"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Selling -->
            <Border Grid.Row="0" Grid.Column="0" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <DockPanel>
                    <TextBlock Text="Top 5 Best Selling Items" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,5"/>
                    <DataGrid ItemsSource="{Binding TopSellingItems}" AutoGenerateColumns="True" IsReadOnly="True" HeadersVisibility="Column"/>
                </DockPanel>
            </Border>

            <!-- Revenue -->
            <Border Grid.Row="0" Grid.Column="1" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <DockPanel>
                    <TextBlock Text="Revenue by Category" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,5"/>
                    <DataGrid ItemsSource="{Binding CategoryRevenues}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Category" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Quantity" Binding="{Binding Value}" Width="Auto"/>
                            <DataGridTextColumn Header="Suma Totala" Binding="{Binding Label}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <!-- Service Time -->
            <Border Grid.Row="1" Grid.Column="0" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Average Service Time" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding AverageServiceTime}" FontSize="32" Foreground="Green" HorizontalAlignment="Center"/>
                    <TextBlock Text="(Time from Order to Served)" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Order History -->
            <Border Grid.Row="1" Grid.Column="1" Margin="5" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <DockPanel>
                    <TextBlock Text="Order History" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,5"/>
                    <DataGrid ItemsSource="{Binding OrderHistory}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="30"/>
                            <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat='dd/MM HH:mm'}" Width="*"/>
                            <DataGridTextColumn Header="Table" Binding="{Binding TableNumber}" Width="50"/>
                            <DataGridTextColumn Header="Total" Binding="{Binding TotalAmount, StringFormat='{}{0:C}'}" Width="80"/>
                            <DataGridTextColumn Header="Payment" Binding="{Binding PaymentMethod}" Width="80"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
