<Window x:Class="RestaurantManager.Wpf.Views.OrderSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Select Items" Height="500" Width="800" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Menu Items -->
        <Grid Grid.Column="0" Margin="0,0,10,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Menu Items" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <ListBox Grid.Row="1" ItemsSource="{Binding MenuItems}" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="5" Padding="5" Width="150" Height="100">
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="Bold" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Category.Name}" FontStyle="Italic" FontSize="10"/>
                                <TextBlock Text="{Binding Price, StringFormat='Price: {0} RON'}" Margin="0,5,0,0"/>
                                <Button Content="Add" Command="{Binding DataContext.AddToOrderCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                        CommandParameter="{Binding}" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- Right: Current Order -->
        <Border Grid.Column="1" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="5" Padding="10" Background="#F9F9F9">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Current Order" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

                <ListView Grid.Row="1" ItemsSource="{Binding OrderItems}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Item" DisplayMemberBinding="{Binding MenuItem.Name}" Width="120"/>
                            <GridViewColumn Header="Qty" DisplayMemberBinding="{Binding Quantity}" Width="40"/>
                            <GridViewColumn Header="Price" DisplayMemberBinding="{Binding PriceAtOrder}" Width="60"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <TextBlock Grid.Row="2" Text="{Binding TotalAmount, StringFormat='Total: {0} RON'}" 
                           FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" Margin="0,10"/>

                <Button Grid.Row="3" Content="Place Order &amp; Occupy Table" Command="{Binding SaveOrderCommand}" 
                        Background="#4CAF50" Foreground="White" FontSize="16" Padding="10" Margin="0,10,0,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
