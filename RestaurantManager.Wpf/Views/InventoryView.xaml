<UserControl x:Class="RestaurantManager.Wpf.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header + Add New -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Inventory Management" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Background="#f0f0f0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding NewIngredientName}" Width="150" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Qty:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding NewIngredientQuantity}" Width="60" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                    <TextBlock Text="Unit:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox SelectedValue="{Binding NewIngredientUnit}" SelectedValuePath="Content" Width="70" Margin="0,0,15,0" VerticalContentAlignment="Center">
                        <ComboBoxItem Content="kg"/>
                        <ComboBoxItem Content="liters"/>
                        <ComboBoxItem Content="pcs"/>
                        <ComboBoxItem Content="g"/>
                        <ComboBoxItem Content="ml"/>
                    </ComboBox>

                    <Button Content="Add Ingredient" Command="{Binding AddIngredientCommand}" Padding="10,5"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- DataGrid Editable -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Ingredients}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="Ingredient Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding StockQuantity}" Width="100"/>
                <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="100"/>
                
                <DataGridTemplateColumn Header="Actions" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Delete" Command="{Binding DataContext.DeleteIngredientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Background="Red" Foreground="White"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="Tip: Edit directly in the grid and click Save." FontStyle="Italic" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Save Changes" Command="{Binding SaveChangesCommand}" Padding="20,10" Background="LightGreen"/>
        </StackPanel>
    </Grid>
</UserControl>
