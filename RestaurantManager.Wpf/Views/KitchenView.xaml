<UserControl x:Class="RestaurantManager.Wpf.Views.KitchenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:RestaurantManager.Wpf.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <conv:StatusColorConverter x:Key="StatusColorConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1" Margin="0,0,0,10" Padding="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Kitchen Monitor" FontSize="24" FontWeight="Bold"/>
                <Button Grid.Column="1" Content="Refresh Data" Command="{Binding RefreshCommand}" Padding="10,5" Background="#DDDDDD"/>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Active Orders Column -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Active Orders" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,5"/>
                
                <ListView Grid.Row="1" ItemsSource="{Binding ActiveOrders}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Status, Converter={StaticResource StatusColorConverter}}" 
                                    BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0">
                                        <TextBlock Text="{Binding Table.TableNumber, StringFormat='Table {0}'}" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{Binding OrderDate, StringFormat='HH:mm:ss'}"/>
                                    </StackPanel>

                                    <ItemsControl Grid.Column="1" ItemsSource="{Binding OrderItems}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock>
                                                    <Run Text="{Binding Quantity}" FontWeight="Bold"/>
                                                    <Run Text="x "/>
                                                    <Run Text="{Binding MenuItem.Name}"/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Status}" FontWeight="Bold" FontSize="14" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding TotalAmount, StringFormat='Total: {0} RON'}" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <!-- Separator -->
            <GridSplitter Grid.Column="1" Width="2" HorizontalAlignment="Center" Background="LightGray"/>

            <!-- History Column -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Order History (Last 20)" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,5"/>

                <ListView Grid.Row="1" ItemsSource="{Binding HistoryOrders}" Background="#F9F9F9">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#DDDDDD" BorderThickness="0,0,0,1" Padding="5" Margin="0,0,0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Table & Time -->
                                    <StackPanel Grid.Column="0" Width="80">
                                        <TextBlock Text="{Binding Table.TableNumber, StringFormat='Table {0}'}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding OrderDate, StringFormat='HH:mm'}" Foreground="Gray" FontSize="11"/>
                                    </StackPanel>

                                    <!-- Details -->
                                    <ItemsControl Grid.Column="1" ItemsSource="{Binding OrderItems}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock FontSize="12">
                                                    <Run Text="{Binding Quantity}"/>
                                                    <Run Text="x"/>
                                                    <Run Text="{Binding MenuItem.Name}"/>
                                                </TextBlock>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <!-- Amount -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                        <TextBlock Text="PAID" Foreground="Gray" FontWeight="Bold" FontSize="10" HorizontalAlignment="Right"/>
                                        <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0} RON'}" FontWeight="Bold" HorizontalAlignment="Right"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
